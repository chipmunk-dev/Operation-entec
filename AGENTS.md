# 저장소 작업 규칙

## 개발 히스토리

- 기능 추가, 오류 수정, UI/UX 변경을 마칠 때마다
  `src/components/Navbar.jsx`의 `changeLogItems`도 같은 작업에서 업데이트한다.
- 최신 월을 위에 두고 같은 월의 작업은 하나의 항목으로 묶는다.
- 날짜는 일수 없이 `YYYY.MM` 형식만 사용한다.
- 여러 작업은 빠짐없이 한 줄씩 기록하되, 각 줄은 짧은 보고식으로 작성한다.
- 각 줄은 `[기능페이지] 개발 기능(또는 수정·삭제 기능) - 작업자` 형식으로
  작성한다. 예: `[해외메일 작성] 작성 포맷 변경 - 정지운 사원`
- 한 작업 내용은 화면에서 줄바꿈하지 않는다. 히스토리 영역은 가장 긴 작업에
  맞춰 가로로 확장하고, 화면 폭이 부족할 때만 가로 스크롤을 허용한다.
- 저장소 관리자가 요청한 작업의 기본 표기는 `정지운 사원`이다.
- `ju00moon-sketch`가 기여한 점등 내역 편집 작업의 표기는 `주상돈 사원`이다.
- 사용자가 다른 개발자를 지정하면 사용자의 지정을 우선한다.
